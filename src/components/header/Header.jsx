import React from 'react';

import './Header.scss';

const Header = () => (
  <header className="app-header">
    <div className="container">
      <h1>GITHUB issues</h1>
    </div>
  </header>
);

export default Header;